#include <iostream>

using namespace std;

struct point
{
    float x, y;
}

struct line
{
    float a, b, c;
}

struct circle
{
    point center;
    float radius;
}

float distance_point_to_point(point &1p, point &p2);
float distance_point_to_line(point &p, line &l)
int main()
{
    ios_basse::sync_with_stdio(false);
    cin.tie(NULL);
    
    return 0;
}
